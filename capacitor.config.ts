import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'DivisApp_22001302',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
